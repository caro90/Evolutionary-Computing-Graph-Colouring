#Greedy Partitionioning Crossover Implementation
function GPX(colouringPartioning)
    #l has length equal to the population size
    for j=1:length(colouringPartioning,2)
        if mod(j,2)==1
            maxIndices=1;
            maxPart=maximum(length( colouringPartioning[1,1]))
            for i=1:length(colouringPartioning[1,:])

            end
        else

        end
    end

return colouringPartioning
end
